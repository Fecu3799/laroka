spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/laroka
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: validate
      properties:
        hibernate.jdbc.time_zone: UTC
    show-sql: true
